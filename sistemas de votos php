<?php
// Conecta no banco
$DB_HOST = '127.0.0.1';
$DB_USER = 'root';
$DB_PASS = '';
$DB_NAME = '';
$DB_PORT = 3306;

$conn = new mysqli();

// Se o formulário for enviado (POST)
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $acao = $_POST["acao"];

    if ($acao == "a") {
        // Insere novo voto com 1 no time_a
        $conn->query("INSERT INTO votos (, ) VALUES (1, 0)");
    } else if ($acao == "b") {
        // Insere novo voto com 1 no time_b

    }
}

// Busca os votos totais somando as colunas
$result = $conn->query("SELECT SUM() AS , SUM() AS  FROM ");
$votos = $result->fetch_assoc();

$timeA = $votos[""] ?? 0;
$timeB = $votos[""] ?? 0;
$total = ;
?>
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Votação com INSERT e SELECT</title>
    <style>
        body { font-family: Arial; background: #f5f5f5; padding: 30px; }
        .caixa { background: #fff; padding: 20px; border-radius: 8px; max-width: 400px; margin: auto; }
        h1 { text-align: center; }
        form { text-align: center; margin-top: 20px; }
        button { margin: 5px; padding: 10px 20px; cursor: pointer; }
        .placar { margin-top: 20px; text-align: center; }
    </style>
</head>
<body>
    <div class="caixa">
        <h1>Votação</h1>
        <form method="post">
            <button type="submit" name="acao" value="a">Votar no Time A</button>
            <button type="submit" name="acao" value="b">Votar no Time B</button>
        </form>

        <div class="placar">
            <p><strong>Time A:</strong> <?php echo $timeA; ?> votos</p>
            <p><strong>Time B:</strong> <?php echo $timeB; ?> votos</p>
            <p><strong>Total:</strong> <?php echo $total; ?> votos</p>
        </div>
    </div>
</body>
</html>
